#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - this prints lower case alphabets
 * Return: 0 if run correctyle or none-zero otherwise
 * */
int main(void)
{
	char n;
	for(n = 'a'; n <= z; n++)
	{
		putchar(n);
	}
	putchar("\n");
	return (0);
}

