#include <stdio.h>


/**
 * main - prints number from from 0 to 9
 * Return: returns 0 always if success
 * */

int main(void)
{
	int n;
	for(n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}

